from math import sin, cos, pi
import matplotlib.pyplot as plt
import matplotlib
 
def kochSnowFlake(l, n):
    x_vertex1 = 0
    y_vertex1 = 0
     
    x_vertex2 = l * cos ( 2 * pi/3)
    y_vertex2 = l * sin(2 * pi/3)
     
    x_vertex3 = l * cos (pi/3)
    y_vertex3 = l * sin(pi/3)
     
    kochCurve(x_vertex1, y_vertex1, x_vertex2, y_vertex2, n)
    kochCurve(x_vertex2, y_vertex2, x_vertex3, y_vertex3, n)
    kochCurve(x_vertex3, y_vertex3, x_vertex1, y_vertex1, n)
     
    return

while True:
  try:
    iter = int(input("Introduce el número de iteraciones: "))
    break
  except ValueError:
      print("Sòlo números enteros...")  
      continue

plt.figure()
plt.axis("off")
kochSnowFlake(1,iter)
plt.title("Triangle de Koch")
#plt.savefig("sfkoch.png",bbox_inches="tight")
